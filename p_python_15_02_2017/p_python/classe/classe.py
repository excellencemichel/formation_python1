#Remarque de class

"""
 Dans le contructeur d'une classe les argument passés après 'self',:
    -s'ils sont nommés, l'instanciation (la création) d'un ob de la classe ne
    neccessite pas de fournir les arguments

    -s'ils ne sont pas des paramètres nommés , l'instanciation de la classe les
    arguments doivent être fourni impérativement (comme un fn qui demande ds arg)
    et ces parametre non nommés doivent être passés en parametre au contstruteur
    avant d'être déclarer dans le corps du contructeur

    -Un parmetre nommé à l'interieur de la () du constructeur et dans le corps
    du constructeur une nouvelle valeur pour le même att c'est la valeur du corps
    du constructeur qui est mintenu



"""
